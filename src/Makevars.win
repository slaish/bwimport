PKG_CPPFLAGS += -I. $(shell Rscript -e "cat(Rcpp:::CxxFlags())")

PKG_LIBS += $(shell "${R_HOME}/bin${R_ARCH_BIN}/Rscript.exe" -e "Rcpp:::LdFlags()") \
            -L"$(R_HOME)/bin$(R_ARCH_BIN)" \
            -lcurl -lz
