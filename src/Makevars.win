PKG_CPPFLAGS += -I. $(shell Rscript -e "Rcpp:::CxxFlags()") $(shell Rscript -e "cat(curl::curl_config()$includes)")
PKG_LIBS += $(shell Rscript -e "cat(curl::curl_config()$libs)") $(shell Rscript -e "Rcpp:::LdFlags()")

